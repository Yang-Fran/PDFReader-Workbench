This directory is no longer used for bundled PDF export tools.

The current PDF export path relies on the native system webview runtime:

- Windows: WebView2 runtime
- macOS: native WebKit printing
- Linux: native WebKitGTK printing

Chromium, Pandoc, and Typst are no longer packaged with the app.
