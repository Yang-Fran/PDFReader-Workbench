; Legacy file kept only to avoid stale references in old environments.
; The current build no longer uses installer hooks for bundled PDF export tools.

!macro NSIS_HOOK_PREINSTALL
!macroend

!macro NSIS_HOOK_POSTINSTALL
!macroend
